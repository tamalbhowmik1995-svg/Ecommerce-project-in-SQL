1. Database Schema and Table Creation
First, create the necessary tables for products, customers, and orders. Each table is designed to store self-contained information, suitable for simple queries
2. Importing Data
You can import data into your tables using the INSERT INTO statement for individual records or specific commands like LOAD DATA INFILE (MySQL) or COPY (PostgreSQL) to import from a file (e.g., CSV).
3. Basic SQL Queries (No Joins)
These queries focus on retrieving, filtering, and summarizing data within a single table using basic SQL commands.
Find the maximum, minimum, and average order amount:
